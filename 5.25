一
《Deep Neural Network Enhanced Sampling-Based Path Planning in 3D Space》 
Jiankun Wang , Member, IEEE, Xiao Jia, Tianyi Zhang, Nachuan Ma , and Max Q.-H. Meng , Fellow, IEEE
IEEE TRANSACTIONS ON AUTOMATION SCIENCE AND ENGINEERING, VOL. 19, NO. 4, OCTOBER 2022

首先，场景是一个3d的空间，机器人可以上下左右前后走
他们用的基本算法是RRT*，RRT*是一个3d空间采样的算法，在所有的空间里随机采样，然后连成一个点
（规划有三种，一个是划分网格的，一个是随机采样的，一个是人工势场）

----------------------------人工势场（拉普拉斯提前标定是不是坑，让机器人不要掉进去////////）
----------------------------划分网格（节省计算资源，靠近障碍物的时候就紧密划分网格，不靠近的时候少一点）
----------------------------PRM，是图搜索，是不是也可以这么看，就是人类一眼就差不多看出来大概是可以怎么走的，给一个启发式的东西

在RRT*里面，因为他们认为太慢了，3维的空间随机采样的，所以他们想到人类大概就是看一眼就知道怎么做规划了，所以让随机采样的变得不随机，局限于某一个区域里面，在这个“正确性大的区域里面找点会更好”，
算法是这样设计的：
  首先，找到了成功的初始路径吗？如果没有，就从DNN预测的H区域里以0.9的概率找，以0.1的概率找全图，如果找到了第一个成功的路径，那就进入优化阶段，以0.5的概率找H区域，以0.5的概率找全图（循环的次数是自己设置的N）
  

这个区域是有一个DNN来确定的，DNN他们设置的是一个通用的模型，要给他喂很多数据，这个数据是这样设计的：
搞了一个80x80x80的图，然后用了A*，找到一条很细的线，然后扩张，以1x1x1的单位变成了3x3x3的单位，


----------------------------Anytime Dynamic RRT，这个是动态的规划，说不定可以结合教师模型进行修改
----------------------------RRT和人工势场结合，A*和RRT结合，说白了就是让点采样得更有启发性，这已经有人做过了，但是是不是可以引入限制函数试试看，避免势场掉进去黑洞
cGAN和CNN：
1.cGAN优化了模型结构，把链接改成了add，简化计算
2.做了一个评价loss的函数设计，结合了cGAN和CNN的损失函数设计

目前来看，他们设计的DNN是找了一个“有希望的区域”，但是在未来也可以加入车子自己的物理性能进行考虑，还可以加入比如动态的，更科学的“有希望区域”质量评估标准

二

《Deep Reinforcement Learning for Safe Local Planning of a Ground Vehicle in Unknown Rough Terrain》   Shirel Josef and Amir Degani , Member, IEEE     
IEEE ROBOTICS AND AUTOMATION LETTERS, VOL. 5, NO. 4, OCTOBER 2020

要应对一个崎岖的环境，这里用的方法是端到端的深度学习的方法，这个和上一个不一样，这个是纯纯靠奖励做出来的，上一个是“有一个目标，然后不断拟合，训练出一个专家”，这里是纯纯奖励自己学，
首先，他们做的是局部动态规划，而且设计的是奖励函数很密集，并且关注可解释性，这个引入了注意力机制

传统的动态局部规划的东西有：DWA，势场，向量场直方图，纯方位导航，但是都无法解决崎岖的路边的问题，这个是一个2.5D的问题
-------------------------------DWA局部收敛不可靠，而且参数需要调整，那直接做成自适应的不就可以了，他这里用的是CNN直接跳过这个部分了
-------------------------------势场他也说不行，又是掉进坑里面
-------------------------------向量场的话他说是

ego‑graph，这个就是每一步我遇到了新的东西，我同时要算好多个“可能可行的路径”，然后我挑一个

他们用的算法是：DQN,这个东西是一个深度强化学习的东西，他是基于一个叫做Q learning的算法进行改进的，他做了双层的在线网络，做了网络化的改进

attention，注意力的机制，这个attention分为了两种：
1.有地图的输入，先提取地图，用CNN提取出来然后和自己的姿态和动作拼接，输出的是热图，然后热图会影响决策
2.没地图，直接提取，那他就是自身姿态、目标方向，最后输出的是权重

重点是Q的这种算法，他用了一个东西叫做rainbow的一个agent，这个东西集成了很多DQ的一堆算法，
针对不同的传感器配置，它有不同的方案：
1.如果只能检测IMU，就是只有一个三位的向量，那它就放到attention里
2，如果你可以判断，那就添加成为六位的向量，
3.最好的是可以检测地面情况的，那就CNN检测地面情况，结合决策器进行判断
并且他还对rainbow进行了调整，他对输入ranbow的数据进行了处理，用了LSTM进行判断是不是在上下坡，这个
输入 → [可选: CNN] → LSTM → Dueling层 → 输出Q值分布
在ranbow里面，做了一个圆形标靶”奖励函数
