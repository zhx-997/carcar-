5.25只读了两篇精度的，
一个讲的是dnn和cgan启发rrt*找3d的路径的
一个是决策器搭建的，分层，首先根据不同的数据分成了三个方案，有LSTM帮助你判断是不是在上下坡，改进过的rainbow进行决策，具体在奖励函数设置成了一个圈圈，attention机制

一，泛读
《M2Diffuser: Diffusion-based Trajectory Optimization for Mobile Manipulation in 3D Scenes》
扩散模型，Diffusion-based   扩散模型和GaN很像，但是它是噪声，不断的去噪，两个人,一个人把图片转换成向量，另一个人把文字转换成向量（这个是训练出来的），我拿到向量了之后，第三个人（UNet 雕刻家）会拿到一个噪声很乱的图，
然后根据向量的信息去雕刻，具体表现在它首先会雕刻第一步，然后每一步都会看图纸（向量）然后去精细打磨，需要调整的参数有seed（随机数）和guidance sacle(是否严格匹配文字的内容)，
100个连续的“动作帧”，这个文章要解决的就是这个问题，每一帧中十个自由度分别长啥样，做好控制
这里就是设计了一个扩散的模型，他训练了一个编码器，编码器的输入是一个点云，输出是一个动作，

她还设计了一个fai,
物理函数：其实就是一套“规则评分卡”和“任务打卡表”：

成本函数：主要关注物理世界的安全（如避免碰撞、保持平滑、不超出关节活动范围）。

能量函数：主要评估任务完成的进度和效果（如抓得稳不稳、放得准不准）。

最终动作：由一系列关节的角度序列组成，

用扩散模型一次性生成整条全身运动轨迹（多自由度、长时域），在推理时用可微分的物理/任务代价函数引导优化，保证安全和高成功率，适用于静态已知环境中的移动操作。

idea:
用扩散模型一次性生成小车在崎岖地形上的安全轨迹，同时用 LSTM 编码历史状态理解上下坡，并在推理时通过可微分的物理代价函数优化，保证不翻车、不撞墙，实现全局最优的离线规划。
